+  });
+});
diff --git a/src/lib/responsiveLayout.ts b/src/lib/responsiveLayout.ts
new file mode 100644
index 0000000..0b4245e
--- /dev/null
+++ b/src/lib/responsiveLayout.ts
@@ -0,0 +1,11 @@
+export type LayoutMode = 'phone' | 'tablet' | 'desktop';
+
+export const PHONE_LAYOUT_MAX_WIDTH = 640;
+export const TABLET_LAYOUT_MAX_WIDTH = 1024;
+
+export function layoutModeForWidth(width: number): LayoutMode {
+  const normalized = Number.isFinite(width) && width > 0 ? width : TABLET_LAYOUT_MAX_WIDTH + 1;
+  if (normalized <= PHONE_LAYOUT_MAX_WIDTH) return 'phone';
+  if (normalized <= TABLET_LAYOUT_MAX_WIDTH) return 'tablet';
+  return 'desktop';
+}
diff --git a/src/main.tsx b/src/main.tsx
index 82938e4..efe1e5b 100644
--- a/src/main.tsx
+++ b/src/main.tsx
@@ -13,6 +13,7 @@ import './guided.css';
 import './presentation.css';
 import './presentation-fit.css';
 import './final-enhancements.css';
+import './mobile.css';
 
 createRoot(document.getElementById('root')!).render(
   <StrictMode>
diff --git a/src/mobile.css b/src/mobile.css
new file mode 100644
index 0000000..96cb8f0
--- /dev/null
+++ b/src/mobile.css
@@ -0,0 +1,356 @@
+/* Mobile-first responsive layer. Imported last so phone/tablet layouts can override the desktop presentation shell safely. */
+
+html,
+body,
+#root { max-width: 100%; }
+
+.advanced-mode-banner {
+  min-width: 0;
+  display: grid;
+  grid-template-columns: minmax(0, 1fr) auto;
+  gap: 12px;
+  align-items: center;
+  margin: 10px 0 12px;
+  border: 3px solid var(--ink);
+  border-radius: 15px;
+  padding: 10px 12px;
+  background: var(--yellow);
+  color: #111;
+  box-shadow: 3px 4px 0 var(--ink);
+}
+.advanced-mode-banner > div:first-child { min-width: 0; display: grid; gap: 3px; }
+.advanced-mode-banner .eyebrow,
+.advanced-mode-banner strong,
+.advanced-mode-banner small { display: block; color: inherit !important; }
+.advanced-mode-banner strong { font-size: .94rem; line-height: 1.15; }
+.advanced-mode-banner small { opacity: .72; line-height: 1.35; }
+.llm-settings select { width: 100%; min-width: 0; padding: 8px; border: 1px solid var(--border); border-radius: 8px; }
+.live-ai-settings select { width: 100%; min-width: 0; padding: 7px; border: 2px solid var(--ink); border-radius: 8px; background: var(--paper-white); color: #111; }
+.llm-enable-toggle { display: inline-flex !important; flex-direction: row !important; align-items: center; gap: 5px; }
+.llm-enable-toggle input { width: auto !important; }
+.llm-connection-status.connected { border-color: var(--good); }
+
+@media (max-width: 1024px) {
+  main[data-layout="tablet"] .presentation-appbar {
+    grid-template-columns: auto minmax(0, 1fr);
+    height: auto;
+  }
+  main[data-layout="tablet"] .presentation-appbar-actions { grid-column: 2; }
